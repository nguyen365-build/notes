# Sketch the graph

1. Sketch the graph of a piecewise function ( f(x) ) and its derivative ( f'(x) ), where

* ( f(x) = 4 ) for ( x \le -2 )
* ( f(x) = x^2 ) for ( -2 \le x \le 3 )
* ( f(x) = \dfrac{2}{x - 3} ) for ( x > 3 )


$$f(x) = \begin{cases} 4 & \text{if } x \le -2 \\ x^2 & \text{if } -2 \le x \le 3 \\ \dfrac{2}{x - 3} & \text{if } x > 3 \end{cases}$$

### 1. Piecewise Function $f(x)$

The function is defined as:

$$
f(x) = \begin{cases} 
4 & \text{if } x \le -2 \\ 
x^2 & \text{if } -2 \le x \le 3 \\ 
\dfrac{2}{x - 3} & \text{if } x > 3 
\end{cases}
$$

### 2. Derivative Function $f'(x)$

The derivative is calculated as:

$$
f'(x) = \begin{cases} 
0 & \text{if } x < -2 \\ 
2x & \text{if } -2 < x < 3 \\ 
-\dfrac{2}{(x - 3)^2} & \text{if } x > 3 
\end{cases}
$$

> **Note:** The derivative is undefined at $x = -2$ (where there is a corner) and $x = 3$ (where there is an infinite discontinuity).


# Derivative of a function

Find the derivative of the function:
[
\frac{d}{dx}\left( x \cos(\sqrt{x - 3}) \right)
]
